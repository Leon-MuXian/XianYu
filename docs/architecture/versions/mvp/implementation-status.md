# 闲遇 MVP 实现状态

文档日期：2026-07-21
状态：阶段一技术基线完成，等待 trial 真实业务阶段门
正式范围：完整 MVP；阶段顺序由 `pilot-wedge.md` 约束

本文件只记录代码事实，不改变产品范围。产品要求仍以 active MVP 版本包为准。

## 端与基础设施

| 范围 | 当前代码事实 | 后续边界 |
| --- | --- | --- |
| Spring Boot API | 统一不透明会话、完整 V1、租户隔离、权益账本、最小预约、OpenAPI、健康检查、审计和幂等已实现 | 阶段二补完整预约变更/取消/候补，阶段三补完整报表 |
| 平台后台 Web | 使用生成契约和共享客户端读取真实事实表，试用调整、客服微信、配置和审计可用 | 阶段三完成真实设备总验收 |
| 店长小程序 | 开店、试点配置、发卡实收、邀请码、最小时段、摘要、预警和冻结链路可运行 | 阶段二按原型拆分完整管理页面 |
| 员工小程序 | 登录、今日服务、空态、名单、到店、核销和冻结链路可运行 | 阶段二补首次资料、记录和个人页 |
| 会员小程序 | 微信登录、邀请码绑定、卡包、最小预约、无卡反馈和冻结链路可运行 | 阶段二补筛选、详情、候补、取消和记录页面 |
| PostgreSQL | 完整 V1 可从空 PostgreSQL 迁移；事实表、约束、索引和小写状态统一 | 无存量数据假设失效时必须新增迁移，不得重写已部署 V1 |
| Compose/Nginx | PostgreSQL、API、Admin Web、Nginx 四服务已完成隔离 smoke，健康检查、SPA 回退和 `/api` 转发通过 | 外部提供 HTTPS 域名和真实微信凭据后部署 trial |
| CI | 文档/71 帧映射、OpenAPI 双向漂移、Maven/Testcontainers、前端测试/类型/构建、迁移和镜像构建均为门禁 | 阶段二增加浏览器和微信开发者工具端到端门禁 |

## 原型追踪

唯一机器可读矩阵是 `prototype-implementation-map.json`，并由 `npm run check:docs` 与原型逐项比对。

| 状态 | 数量 | 含义 |
| --- | ---: | --- |
| `implemented` | 37 | 已有可运行页面或页面状态 |
| `pilot_implemented` | 9 | 试点链路已在合并页面可操作，完整信息架构待后续阶段 |
| `backend_ready` | 12 | 数据和 API 已具备，独立页面待阶段二/三 |
| `planned` | 13 | 受真实业务阶段门约束，尚未开始 |
| 合计 | 71 | 与 active HTML 原型严格一致 |

追踪完成不等于 71 个画板全部实现。当前阶段一对应的 45 个画板/状态均已达到 `implemented` 或 `pilot_implemented`；阶段二、三条目保留真实状态，不提前宣称完成。

## 阶段门

阶段一技术验收不自动授权阶段二。进入完整预约前必须完成 trial 部署，并取得 `pilot-wedge.md` 定义的访谈、真实发卡、真实核销和持续使用信号。技术证据和待取得的外部证据记录在 `../../../exec-plans/active/mvp-governance/trial-gate.md`。
