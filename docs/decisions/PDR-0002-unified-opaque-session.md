# PDR-0002 四端统一不透明会话

状态：accepted
日期：2026-07-21
关联版本：mvp

## 背景

平台后台已有数据库会话，完整 MVP 还需要店长、员工和会员认证、撤销、冻结校验和租户上下文。

## 决策

四端统一使用数据库支持的不透明 Bearer token。原始 token 只返回客户端，数据库保存 SHA-256 哈希，并记录角色、主体、租户、过期和撤销状态。

## 影响

所有业务请求从会话解析角色和租户。微信仅用 `code2session` 获取 openid；本地测试替身不得在 trial 或 prod 启用。
