# PDR-0001 重写 MVP 数据库基线

状态：accepted
日期：2026-07-21
关联版本：mvp

## 背景

当前 V1 只覆盖平台后台，字段类型、状态值和租户约束与完整 MVP 数据设计不一致。已确认没有需要保留的 trial 或 prod 数据。

## 决策

直接重写 V1 为完整 MVP 基线，不为旧后台表结构编写兼容迁移。实施和部署只提供显式重建说明，不自动删除数据库或卷。

## 影响

本地已有数据库必须由操作者明确重建。新基线统一使用小写状态、`timestamptz`、`jsonb`、金额精度、租户字段、外键、唯一约束和关键索引。
