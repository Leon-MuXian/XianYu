# PDR-0003 阶段一前置最小会员预约

状态：accepted
日期：2026-07-21
关联版本：mvp

## 背景

阶段一需要产生真实到店和核销事实，而正式数据模型要求履约记录关联预约。允许无预约核销会制造临时模型和后续迁移。

## 决策

阶段一提前交付会员查看可约时段和确认预约的最短链路。筛选、候补、复杂取消和完整预约体验仍在阶段二交付。

## 影响

阶段一即可使用标准 `BookableSlot`、`Booking`、`AttendanceConfirm` 和 `DeductionRecord`，无需临时履约表或可空预约外键。
